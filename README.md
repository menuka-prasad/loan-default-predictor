## Backend
1. goto backend folder
2. create virtual environment if not created yet - `python -m venv .venv`
3. activate virtual environment = `.venv\Scripts\activate`
4. run backend = `uvicorn app.main:app --reload`


